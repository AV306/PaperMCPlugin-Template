plugins {
    id 'java'
}


group 'exampleplugin'
version '0.2.1-SNAPSHOT'


repositories {
    mavenCentral()

    // Added Spigot repository (out-of-date, TODO: update)
    /*maven {
        name = 'Spigot'
        url = 'https://hub.spigotmc.org/nexus/content/repositories/snapshots/'
    }*/

    // Added md5's repository for the Bungeecord-Chat api (out-of-date)
    /*maven {
        name = 'Bungeecord-Chat'
        url = 'https://oss.sonatype.org/content/repositories/snapshots/'
    }*/

    // Added PaperMC API
    // Paper implements Spigot and all ts predecessors
    maven {
        name = 'Paper'
        url = "https://papermc.io/repo/repository/maven-public/"
    }

}


dependencies {
    compileOnly 'io.papermc.paper:paper-api:${project.paper_api_version}-R0.1-SNAPSHOT'
}


java {
	toolchain {
        // Minecraft 1.17+ uses Java 17.
		languageVersion = JavaLanguageVersion.of( 17 )
	}
}
